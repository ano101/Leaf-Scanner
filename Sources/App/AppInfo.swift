import Foundation

/// Имя и идентификатор вынесены сюда, чтобы переименование продукта
/// не разбредалось по коду и оставалось правкой одного файла.
public enum AppInfo {
    public static let name = "Leaf"
    public static let bundleIdentifier = "ru.kotliar.leaf"
}
